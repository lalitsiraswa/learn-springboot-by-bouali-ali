spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/learn_spring_boot
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true